'use client';

import Scene from './Scene';

// import dynamic from 'next/dynamic';

// const Scene = dynamic(() => import('./Scene'), { ssr: false, });

export default function NotFoundClient() { return <Scene />; }